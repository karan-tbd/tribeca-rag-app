# Supabase Edge Functions Environment Variables (example)
# Copy to supabase/.env and fill in real values. Do NOT commit real secrets.

# Local development (Supabase CLI)
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_SERVICE_ROLE_KEY=your-local-service-role-key

# Optional local aliases (the helper prefers *_LOCAL when present)
SUPABASE_URL_LOCAL=http://127.0.0.1:54321
SUPABASE_SERVICE_ROLE_KEY_LOCAL=your-local-service-role-key

# Cloud project (comment out for local dev)
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=your-cloud-service-role-key

# OpenAI / Embeddings
OPENAI_API_KEY=
EMBEDDING_MODEL_FALLBACK=text-embedding-3-small

# PDF Processing Configuration
MAX_CHUNK_SIZE=1000
CHUNK_OVERLAP=200
MAX_CONCURRENT_CHUNKS=5

